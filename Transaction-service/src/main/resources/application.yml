spring:
    application:
        name: Transaction-service
    datasource:
        password: aishnapathak
        url: jdbc:postgresql://localhost:5432/transaction
        username: aishnapathak
        driver-class-name: org.postgresql.Driver
    jpa:
        database: postgresql
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: create-drop
        show-sql: true
    security:
        user:
            name: ${USER_NAME:user} # Default to "user" if the environment variable is not set
            password: ${USER_PASSWORD:defaultPassword} # Default to "defaultPassword" if the environment variable is not set

server:
    port: 8082
user-service:
        base-url: http://localhost:8080
        auth:
            username: aishnapathak
            password: aishnapathak
logging:
    level:
        org.springframework.web.client.RestTemplate: DEBUG
        org.springframework.http.client.HttpComponentsClientHttpRequestFactory: DEBUG

